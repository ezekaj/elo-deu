# Minimal LiveKit Configuration for Local Testing
port: 7880
bind_addresses:
  - 0.0.0.0

rtc:
  tcp_port: 7881
  use_external_ip: false  # Disable for local testing
  
room:
  auto_create: true
  empty_timeout: 300

keys:
  devkey: secret

logging:
  level: info